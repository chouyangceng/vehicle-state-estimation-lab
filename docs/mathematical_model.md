# Mathematical Model

采用车辆纵向速度 `vx`、横向速度 `vy`、横摆率 `r` 和横摆角 `psi` 作为动力学自行车模型状态。前后轮侧偏角由车辆速度、横摆率和轴距计算，轮胎侧向力使用 Fiala 非线性模型，并通过 `mu * Fz` 限制最大附着力。

滤波器将车辆模型作为预测模型，IMU、GNSS、轮速和转角作为观测。所有矩阵运算都在代码中检查维度和有限值，协方差在更新后对称化并截断极小负特征值。
